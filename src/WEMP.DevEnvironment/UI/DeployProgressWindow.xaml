<Window x:Class="WEMP.DevEnvironment.UI.DeployProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="正在部署"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        Background="{StaticResource WindowBackgroundBrush}"
        FontFamily="{StaticResource AppFont}">

    <Grid Margin="24">
        <StackPanel>
            <TextBlock Text="正在部署" FontSize="18" FontWeight="Bold" Foreground="{StaticResource TextPrimaryBrush}" />
            <TextBlock Margin="0,4,0,0" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                       Text="{Binding DeployTarget, Mode=OneWay}" TextTrimming="CharacterEllipsis" />

            <ProgressBar Height="8" Margin="0,20,0,0" Maximum="100" Value="{Binding ProgressPercent, Mode=OneWay}"
                         Foreground="{StaticResource AccentBrush}" Background="{StaticResource ControlBackgroundBrush}" BorderThickness="0" />

            <DockPanel Margin="0,10,0,0">
                <TextBlock DockPanel.Dock="Right" FontSize="12" Foreground="{StaticResource TextTertiaryBrush}">
                    <Run Text="{Binding ProgressPercent, Mode=OneWay}" /><Run Text="%" />
                </TextBlock>
                <TextBlock FontSize="12" Foreground="{StaticResource TextSecondaryBrush}"
                           Text="{Binding ProgressText, Mode=OneWay}" TextTrimming="CharacterEllipsis" />
            </DockPanel>
        </StackPanel>
    </Grid>
</Window>
