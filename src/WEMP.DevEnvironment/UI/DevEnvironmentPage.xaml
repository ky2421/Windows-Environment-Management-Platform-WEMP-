<UserControl x:Class="WEMP.DevEnvironment.UI.DevEnvironmentPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <TextBlock Text="开发环境" FontSize="22" FontWeight="Bold" VerticalAlignment="Center" />
            <TextBlock DockPanel.Dock="Right" Text="{Binding Status}" VerticalAlignment="Center" Foreground="#666666" />
        </DockPanel>

        <TextBlock Grid.Row="1" Margin="0,6,0,0" Foreground="#666666" FontSize="12"
                   Text="基于 YAML 模板一键部署工具链：工具安装 → 环境变量 → 配置文件 → 验证 → 快照。" />

        <Grid Grid.Row="2" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*" />
                <ColumnDefinition Width="20" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>

            <!-- 左：模板库 -->
            <DockPanel Grid.Column="0">
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <Button Content="部署选中模板" Padding="12,4" DockPanel.Dock="Right"
                            Command="{Binding DeployCommand}" />
                    <TextBlock Text="模板库" FontWeight="SemiBold" VerticalAlignment="Center" />
                </DockPanel>
                <ListBox ItemsSource="{Binding Templates}" SelectedItem="{Binding SelectedTemplate}"
                         BorderBrush="#DDDDDD">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock FontWeight="SemiBold">
                                    <Run Text="{Binding Name, Mode=OneWay}" /><Run Text="  v" /><Run Text="{Binding Version, Mode=OneWay}" />
                                </TextBlock>
                                <TextBlock FontSize="11" Foreground="#888888" Text="{Binding Description}" TextWrapping="Wrap" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <!-- 右：实例列表 -->
            <DockPanel Grid.Column="2">
                <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                        <Button Content="验证" Padding="12,4" Margin="4,0,0,0"
                                Command="{Binding ValidateCommand}" CommandParameter="{Binding SelectedInstance}" />
                        <Button Content="回滚" Padding="12,4" Margin="4,0,0,0"
                                Command="{Binding RollbackCommand}" CommandParameter="{Binding SelectedInstance}" />
                    </StackPanel>
                    <TextBlock Text="部署实例" FontWeight="SemiBold" VerticalAlignment="Center" />
                </DockPanel>
                <ListBox ItemsSource="{Binding Instances}" SelectedItem="{Binding SelectedInstance}"
                         BorderBrush="#DDDDDD">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="2">
                                <TextBlock FontWeight="SemiBold">
                                    <Run Text="{Binding Name, Mode=OneWay}" /><Run Text="  ·  " />
                                    <Run Text="{Binding Status, Mode=OneWay}" Foreground="#666666" />
                                </TextBlock>
                                <TextBlock FontSize="11" Foreground="#888888">
                                    <Run Text="{Binding Template.Name, Mode=OneWay}" /><Run Text="  部署于 " />
                                    <Run Text="{Binding DeployedAt, Mode=OneWay, StringFormat={}{0:yyyy-MM-dd HH:mm}}" />
                                </TextBlock>
                                <ItemsControl ItemsSource="{Binding Tools}" Margin="0,4,0,0">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock FontSize="11" Foreground="#666666">
                                                <Run Text="{Binding ToolName, Mode=OneWay}" /><Run Text="  " />
                                                <Run Text="{Binding Status, Mode=OneWay}" />
                                            </TextBlock>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>
        </Grid>
    </Grid>
</UserControl>
